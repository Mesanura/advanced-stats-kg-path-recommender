# 贡献规范

## 分支

- 功能：`feat/<简短名称>`
- 修复：`fix/<简短名称>`
- 文档：`docs/<简短名称>`

## 提交

提交遵循 `<type>: <中文说明>`，常用类型包括 `feat`、`fix`、`test`、`docs`、`refactor`、`build` 和 `chore`。

每个提交只处理一个功能或问题，并包含对应测试。合并前必须通过后端、前端和端到端测试中与变更相关的部分。

## 安全

禁止提交真实账号密码、JWT 密钥、`.env`、运行数据库、项目要求、报告模板或项目报告。

